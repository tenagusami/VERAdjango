{% load static %}

<link rel="stylesheet" type="text/css" href="{% static 'vlbi_schedule/style.css' %}" />

<h1>VERA小笠原局</h1>

{% if error_message %}<p><strong>{{ error_message }}</strong></p>{% endif %}

<form action="{% url 'vlbi_schedule:result' year doy %}" method="post">
  {% csrf_token %}
  <table border="1" width="500">
    <tr align="center">
      <td colspan="5" bgcolor="lightgray">観測運用</td>
    </tr>
    　<tr>
      　　<th>観測</th>
      　　<th>ターゲット</th>
      <th>バンド</th>
      <th>PI</th>
      <th>局対応・備考</th>
      　</tr>
    
    {% for schedule in schedule_list %}
    　<tr align="center">
      　　<td>{{schedule.ID}}</td>
      　　<td>{{schedule.description}}</td>
      <td>{{schedule.band}}</td>
      <td>{{schedule.PI_name}}</td>
      <td></td>
      　</tr>
    {% empty %}
    <tr align="center"><td>　</td><td></td><td></td><td></td><td></td></tr>
    {% endfor %}
  </table>
  <br>
  year:<input type="text" value="{{year}}" name="year" maxlength=4 ><br>
  day of year: <input type="text" value="{{doy}}" name="doynew" maxlength=3 ><br>
  <input type="submit" value="Query" />
</form>
</html>
